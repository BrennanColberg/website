import styles from '../styles/Logo.module.scss'

const Logo = () => (
  <div className={styles.logo}>
    <div className={styles.headshot} />
    <h1 className={styles.name}>Brennan Colberg</h1>
  </div>
)

export default Logo
